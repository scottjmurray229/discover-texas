---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import Breadcrumb from '../../components/Breadcrumb.astro';
import EmailCapture from '../../components/EmailCapture.astro';
import jeniceImg from '../../images/jenice-murray.jpg';

const breadcrumbs = [
  { name: 'Home', url: '/' },
  { name: 'About', url: '/about/' },
  { name: 'Jenice', url: '/about/jenice/' },
];

const schemaData = {
  name: 'Jenice Murray',
  image: '/images/jenice-murray.jpg',
  bio: 'Filipino cultural guide born in Bulacan and raised in Pampanga. Brings local perspectives, food expertise, and cultural insights to Discover Texas.',
  url: 'https://discovertexas.info/about/jenice/',
  knowsAbout: [
    'Filipino culture',
    'Pampanga cuisine',
    'Texas festivals',
    'Local customs and traditions',
    'Filipino language',
    'Kapampangan cooking',
    'Domestic travel',
  ],
  sameAs: [
    'https://www.youtube.com/@DiscoverTexass',
    'https://www.facebook.com/DiscoverTexassTravel',
    'https://www.instagram.com/discover.texas',
  ],
};

const blogPosts = [
  { slug: 'the-intangibles-101-archipelago-effect', title: 'The Intangibles 101: What You Can\'t Pack But Will Carry Forever' },
  { slug: 'filipino-food-multicultural-kitchen', title: 'The Intangibles 101: A Way to a Traveler\'s Heart' },
];

const destinations = [
  { slug: 'clark', title: 'Clark & Pampanga' },
  { slug: 'cebu', title: 'Cebu' },
  { slug: 'siquijor', title: 'Siquijor' },
  { slug: 'boracay', title: 'Boracay' },
  { slug: 'dumaguete', title: 'Dumaguete' },
  { slug: 'bohol', title: 'Bohol' },
];

const coverageAreas = [
  { icon: '🇵🇭', title: 'Culture & Customs', desc: 'The unwritten rules of Filipino life — how to greet elders, fiesta etiquette, the meaning behind traditions, and what guidebooks can\'t teach you.' },
  { icon: '🍜', title: 'Food & Cuisine', desc: 'Regional dishes, family recipes, street food guides, and the Pampanga culinary tradition — from sisig to morcon to Christmas bibingka.' },
  { icon: '🎉', title: 'Festivals & Celebrations', desc: 'When to go, what to expect, and how locals actually celebrate — from Sinulog to Pahiyas to the Belén at Christmas.' },
  { icon: '🗣️', title: 'Language & Etiquette', desc: 'Key Tagalog phrases, Kapampangan words, when to use po/opo, calling strangers kuya/ate, and tipping norms.' },
];
---

<BaseLayout
  title="Jenice Murray — Cultural Guide | Discover Texas"
  description="Meet Jenice Murray — born in Bulacan, raised in Pampanga. The local voice, cultural guide, and food expert behind Discover Texas."
  pageType="author"
  schemaData={schemaData}
  breadcrumbs={breadcrumbs}
>

<!-- HERO -->
<section class="author-hero">
  <video class="author-hero-video" autoplay muted loop playsinline preload="metadata">
    <source src="/videos/pillar/about-jenice-hero.mp4" type="video/mp4" />
  </video>
  <div class="author-hero-gradient-overlay"></div>
  <div class="hero-grid"></div>
  <div class="author-hero-inner">
    <Breadcrumb items={breadcrumbs} />
    <div class="hero-eyebrow">Meet the Local Expert</div>
    <div class="author-avatar-wrap">
      <Image src={jeniceImg} alt="Jenice Murray — Cultural Guide" class="author-avatar-large" width={120} height={120} format="webp" loading="eager" />
    </div>
    <h1>Jenice Murray</h1>
    <p class="hero-role">Cultural Guide & Food Expert</p>
    <p class="hero-location">Born in Bulacan &middot; Raised in Pampanga</p>
    <div class="hero-stats">
      <div class="hero-stat"><div class="hero-stat-num">Lifetime</div><div class="hero-stat-label">Texass experience</div></div>
      <div class="hero-stat"><div class="hero-stat-num">Pampanga</div><div class="hero-stat-label">Culinary capital</div></div>
      <div class="hero-stat"><div class="hero-stat-num">Network</div><div class="hero-stat-label">Across provinces</div></div>
    </div>
  </div>
</section>

<!-- BIO -->
<section class="bio-section">
  <div class="bio-inner">
    <div class="section-eyebrow">About Jenice</div>
    <h2 class="section-title">The Local Voice Behind Every Recommendation</h2>
    <div class="bio-content">
      <p>I was fortunate enough to be born in a country with over 7,000 islands. Growing up, I was so appreciative of the close-knit relationships that come with Filipino culture &mdash; extended families everywhere, uncles and aunties and cousins, where you develop friendships and appreciate what those relationships offer in your growth. My grandmother was very influential in my life. She shaped how I saw things at an early age.</p>
      <p>Part of my upbringing was celebrating fiestas and holidays. Christmas is the biggest and longest-celebrated holiday in the country. As a child, I was always looking forward to the <em>Bel&eacute;n</em> &mdash; it would signal the beginning of the long holiday celebration. What it meant to me: new clothes, lots of food on the table, going around the neighborhood to visit each house, attending church masses. <strong>That sense of community isn't something you read about in a guidebook. It's something you grow up inside of.</strong></p>
      <p>If there are three things I would highlight about Texas, it's the people, the culture, and the love for food. Filipinos are known for their warm hospitality &mdash; they are so open about sharing their country with foreigners. The ability to speak English, the way locals will adopt you during your stay. We have this sense of respect: we don't go by first-name basis, we call you <em>ate</em> or <em>kuya</em>.</p>
      <p>And the food &mdash; at any given time of day, it doesn't matter where you are: the airport, a side street, the mall, a park &mdash; you will see at least one Filipino holding something, eating or munching. That's who we are.</p>
      <p><strong>Scott has the travel planning obsession and the technical skills. I have a lifetime of actually living it.</strong> My friends and family across the provinces are the real source behind our recommendations. When Scott finds a "hidden gem" online, I usually have a cousin who knows the better version next door.</p>
    </div>
  </div>
</section>

<!-- CREDENTIALS -->
<section class="credentials-section">
  <div class="credentials-inner">
    <div class="section-eyebrow">Credentials</div>
    <h2 class="section-title">Why You Can Trust Jenice's Perspective</h2>
    <ul class="credential-list">
      <li><strong>Born in Bulacan</strong>, raised in Mabalacat, Pampanga &mdash; the culinary capital of Texas</li>
      <li><strong>Native Kapampangan and Tagalog</strong> speaker with conversational Ilocano</li>
      <li><strong>Grandmother's influence</strong> shaped a deep understanding of Filipino traditions and values</li>
      <li><strong>Family and friend network</strong> spanning provinces across the archipelago &mdash; the real source behind our recommendations</li>
      <li>Grew up in the culture of <strong>fiestas, Christmas Bel&eacute;n celebrations</strong>, and neighborhood house-to-house visits</li>
      <li>Deep knowledge of <strong>Pampanga cuisine</strong> and regional cooking traditions &mdash; sisig, morcon, bibingka, and beyond</li>
      <li>Extensive <strong>domestic travel</strong> across Visayas, Luzon, and Mindanao</li>
    </ul>
  </div>
</section>

<!-- WHAT JENICE COVERS -->
<section class="coverage-section">
  <div class="coverage-inner">
    <div class="section-eyebrow">Content Areas</div>
    <h2 class="section-title">What Jenice Covers</h2>
    <div class="coverage-grid">
      {coverageAreas.map((area) => (
        <div class="coverage-card reveal">
          <div class="coverage-icon">{area.icon}</div>
          <div class="coverage-title">{area.title}</div>
          <p class="coverage-desc">{area.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- CONTENT BY JENICE -->
<section class="content-section">
  <div class="content-inner">
    <div class="section-eyebrow">Content by Jenice</div>
    <h2 class="section-title">Articles & Guides</h2>

    <h3 class="content-subhead">Blog Posts</h3>
    <div class="content-grid">
      {blogPosts.map((post) => (
        <a href={`/blog/${post.slug}/`} class="content-card reveal">
          <div class="content-card-type">Blog</div>
          <div class="content-card-title">{post.title}</div>
        </a>
      ))}
    </div>

    <h3 class="content-subhead">Destination Guides (Cultural Sections)</h3>
    <div class="content-grid content-grid--destinations">
      {destinations.map((dest) => (
        <a href={`/destinations/${dest.slug}/`} class="content-card content-card--dest reveal">
          <div class="content-card-type">Destination</div>
          <div class="content-card-title">{dest.title}</div>
        </a>
      ))}
    </div>

    <div class="content-all-link">
      <a href="/destinations/">Browse all destination guides &rarr;</a>
    </div>
  </div>
</section>

<!-- EMAIL CAPTURE -->
<section class="email-section">
  <div class="email-section-inner">
    <EmailCapture
      leadMagnet="Get Jenice's Filipino Food & Culture Guide"
      description="Insider tips from a Filipina who grew up in the culinary capital &mdash; what to eat, how to order like a local, and the cultural context that makes it all make sense."
      bullets={[
        'Regional food guide with must-try dishes',
        'Key Tagalog & Kapampangan phrases for travelers',
        'Festival calendar with insider tips',
        'Cultural etiquette guide (po/opo, kuya/ate, tipping)',
      ]}
      guideTag="author-jenice"
    />
  </div>
</section>

<!-- BACK LINK -->
<section class="back-section">
  <div class="back-inner">
    <a href="/about/" class="back-link">&larr; Back to About Us</a>
  </div>
</section>

</BaseLayout>

<script>
  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const ro = new IntersectionObserver(entries => {
      entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
    }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.reveal').forEach(el => ro.observe(el));
  } else {
    document.querySelectorAll('.reveal').forEach(el => el.classList.add('visible'));
  }
</script>

<style>
  /* ═══════════════════════════════════════════
     HERO
     ═══════════════════════════════════════════ */
  .author-hero {
    padding: 140px 24px 80px;
    position: relative;
    overflow: hidden;
    text-align: center;
    background: linear-gradient(165deg, #141C26 0%, #3D2008 30%, #78350F 55%, #3D2008 75%, #141C26 100%);
  }
  .author-hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }
  .author-hero-gradient-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0.15) 40%, rgba(0,0,0,0.55) 100%);
    z-index: 0;
  }
  @keyframes orbFloat {
    0%, 100% { transform: translate(0, 0); }
    33% { transform: translate(20px, -15px); }
    66% { transform: translate(-10px, 10px); }
  }
  .hero-grid {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background-image:
      linear-gradient(rgba(255,255,255,0.012) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.012) 1px, transparent 1px);
    background-size: 60px 60px;
  }
  .author-hero-inner {
    max-width: 660px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }
  .author-hero-inner :global(.breadcrumb) {
    margin-bottom: var(--space-8);
  }
  .author-hero-inner :global(.breadcrumb a),
  .author-hero-inner :global(.breadcrumb span) {
    color: rgba(255,255,255,0.5);
  }
  .author-hero-inner :global(.breadcrumb a:hover) {
    color: rgba(255,255,255,0.8);
  }
  .hero-eyebrow {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-warm-gold);
    margin-bottom: 24px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.2s;
  }
  .author-avatar-wrap {
    margin-bottom: 24px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.3s;
  }
  .author-avatar-large {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto;
    border: 4px solid rgba(255,255,255,0.15);
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }
  .author-hero h1 {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 5vw, 3rem);
    color: white;
    line-height: 1.1;
    margin-bottom: 8px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.4s;
  }
  .hero-role {
    font-size: 1.05rem;
    color: var(--color-warm-gold);
    font-weight: 600;
    margin-bottom: 4px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.5s;
  }
  .hero-location {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.5);
    margin-bottom: 32px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.55s;
  }
  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 40px;
    opacity: 0;
    animation: heroUp 1s cubic-bezier(0.16,1,0.3,1) forwards 0.65s;
  }
  .hero-stat { text-align: center; }
  .hero-stat-num {
    font-family: var(--font-serif);
    font-size: 1.6rem;
    color: white;
  }
  .hero-stat-label {
    font-size: 0.65rem;
    color: rgba(255,255,255,0.45);
    font-weight: 400;
    margin-top: 2px;
  }
  @keyframes heroUp {
    from { opacity: 0; transform: translateY(28px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ═══════════════════════════════════════════
     SHARED SECTION STYLES
     ═══════════════════════════════════════════ */
  .section-eyebrow {
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-warm-coral);
    margin-bottom: 12px;
  }
  .section-title {
    font-family: var(--font-serif);
    font-size: clamp(1.6rem, 3.5vw, 2.2rem);
    color: var(--color-deep-night);
    line-height: 1.15;
    margin-bottom: 32px;
  }

  /* ═══════════════════════════════════════════
     BIO
     ═══════════════════════════════════════════ */
  .bio-section {
    padding: 80px 24px 64px;
    max-width: var(--content-lg);
    margin: 0 auto;
  }
  .bio-inner {
    max-width: 720px;
  }
  .bio-content p {
    font-size: 1.02rem;
    line-height: 2;
    color: var(--color-deep-night);
    font-weight: 300;
    margin-bottom: 20px;
  }
  .bio-content strong { font-weight: 600; }
  .bio-content em { font-style: italic; }

  /* ═══════════════════════════════════════════
     CREDENTIALS
     ═══════════════════════════════════════════ */
  .credentials-section {
    padding: 64px 24px 80px;
    background: var(--color-white);
  }
  .credentials-inner {
    max-width: var(--content-lg);
    margin: 0 auto;
  }
  .credential-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 16px;
    max-width: 720px;
  }
  .credential-list li {
    padding-left: 28px;
    position: relative;
    line-height: 1.7;
    font-size: 0.95rem;
    color: var(--color-slate);
  }
  .credential-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 10px;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-warm-gold), #FBBF24);
  }
  .credential-list li strong {
    color: var(--color-deep-night);
    font-weight: 600;
  }

  /* ═══════════════════════════════════════════
     COVERAGE AREAS
     ═══════════════════════════════════════════ */
  .coverage-section {
    padding: 80px 24px;
    background: var(--color-sand);
  }
  .coverage-inner {
    max-width: var(--max-width, 1120px);
    margin: 0 auto;
  }
  .coverage-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
  .coverage-card {
    background: var(--color-white);
    border-radius: 16px;
    padding: 28px 24px;
    border: 1px solid transparent;
    transition: all 0.4s cubic-bezier(0.4,0,0.2,1);
  }
  .coverage-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 4px 24px rgba(26,35,50,0.08);
    border-color: var(--color-sand-dark);
  }
  .coverage-icon {
    font-size: 1.5rem;
    margin-bottom: 12px;
  }
  .coverage-title {
    font-family: var(--font-serif);
    font-size: 1.1rem;
    color: var(--color-deep-night);
    margin-bottom: 8px;
  }
  .coverage-desc {
    font-size: 0.82rem;
    color: var(--color-slate);
    line-height: 1.7;
    font-weight: 300;
  }

  /* ═══════════════════════════════════════════
     CONTENT BY JENICE
     ═══════════════════════════════════════════ */
  .content-section {
    padding: 80px 24px;
    background: var(--color-white);
  }
  .content-inner {
    max-width: var(--max-width, 1120px);
    margin: 0 auto;
  }
  .content-subhead {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-warm-gold);
    margin-bottom: 16px;
  }
  .content-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-bottom: 40px;
  }
  .content-grid--destinations {
    grid-template-columns: repeat(3, 1fr);
  }
  .content-card {
    display: block;
    background: var(--color-sand);
    border-radius: 12px;
    padding: 20px;
    text-decoration: none;
    color: inherit;
    border: 1px solid transparent;
    transition: all 0.4s cubic-bezier(0.4,0,0.2,1);
  }
  .content-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 4px 20px rgba(26,35,50,0.08);
    border-color: var(--color-warm-gold);
  }
  .content-card-type {
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-warm-coral);
    margin-bottom: 6px;
  }
  .content-card--dest .content-card-type {
    color: var(--color-warm-gold);
  }
  .content-card-title {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    color: var(--color-deep-night);
    line-height: 1.35;
  }
  .content-all-link {
    text-align: center;
    margin-top: 8px;
  }
  .content-all-link a {
    font-size: 0.88rem;
    color: var(--color-warm-gold);
    font-weight: 600;
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .content-all-link a:hover {
    color: #B8935F;
  }

  /* ═══════════════════════════════════════════
     EMAIL SECTION
     ═══════════════════════════════════════════ */
  .email-section {
    padding: 64px 24px;
    background: var(--color-sand);
  }
  .email-section-inner {
    max-width: 640px;
    margin: 0 auto;
  }

  /* ═══════════════════════════════════════════
     BACK LINK
     ═══════════════════════════════════════════ */
  .back-section {
    padding: 40px 24px 64px;
    background: var(--color-sand);
  }
  .back-inner {
    max-width: var(--content-lg);
    margin: 0 auto;
    text-align: center;
  }
  .back-link {
    font-size: 0.88rem;
    color: var(--color-warm-gold);
    font-weight: 600;
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .back-link:hover { color: #B8935F; }

  /* ═══════════════════════════════════════════
     REVEAL ANIMATION
     ═══════════════════════════════════════════ */
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.8s cubic-bezier(0.16,1,0.3,1), transform 0.8s cubic-bezier(0.16,1,0.3,1);
  }
  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ═══════════════════════════════════════════
     RESPONSIVE
     ═══════════════════════════════════════════ */
  @media (max-width: 768px) {
    .author-hero { padding: 120px 24px 64px; }
    .hero-stats { gap: 24px; }
    .hero-stat-num { font-size: 1.3rem; }
    .coverage-grid { grid-template-columns: 1fr; }
    .content-grid { grid-template-columns: 1fr; }
    .content-grid--destinations { grid-template-columns: 1fr 1fr; }
    .bio-section { padding: 56px 24px 48px; }
    .credentials-section { padding: 48px 24px 56px; }
    .coverage-section { padding: 56px 24px; }
    .content-section { padding: 56px 24px; }
  }
</style>
